MODE=live
KALSHI_ENV=production
KALSHI_API_KEY_ID=
KALSHI_PRIVATE_KEY_B64=
TRADING_ENABLED=false
EXECUTION_STRATEGY=strike_ruler
# Shared per-15-minute-market cap, including an entry fee cushion; hard maximum 5.
MARKET_BUDGET_DOLLARS=5
# Entry limit:exit target pairs in outcome cents, for every Strike Ruler route.
ENTRY_EXIT_PAIRS_CENTS=32:39,39:46
# First two minutes: one Strike Ruler bias side at 52c, target 60c, then cancel at 2:00.
OPENING_BIAS_ENABLED=true
OPENING_BIAS_PAIR_CENTS=52:60
OPENING_WINDOW_MINUTES=2
# Independent exit monitor. Entry lookups and POLL_SECONDS do not delay it.
EXIT_POLL_SECONDS=1
# Desired principal per trigger, split between both tiers.
# A dual batch splits this one amount across both sides and both tiers.
# Every route shares the remaining market allowance, including fee reserves.
ENTRY_BUDGET_DOLLARS=0.77
MAX_PURCHASES_PER_MARKET=7
ENTRY_INTERVAL_SECONDS=7
ENTRY_START_MINUTE=2
ENTRY_END_MINUTE=5
# Early buys stop by 5:00 and expire at 6:00. Separate late routes use 12:00-15:00.
SPOT_ENTRY_WINDOW_MINUTES=2
SPOT_ENTRY_THRESHOLD_DOLLARS=80
DUAL_LIMIT_BUYS_ENABLED=true
# Final three minutes: wait for the favorite ask to reach 85c, then buy LIMIT;
# it expires at contract close and has a 92c take-profit target.
# Exact observed price required; skip jumps past it. Limit caps execution price.
LATE_PROBABILITY_ENABLED=true
LATE_PROBABILITY_PAIR_CENTS=85:92
# Independently wait for each side's ask to reach 73c before submitting.
LATE_DUAL_PAIR_CENTS=73:81
LATE_ENTRY_START_MINUTE=12
LATE_ENTRY_END_MINUTE=15
ABSOLUTE_GAP_AVERAGE=59.58
POLL_SECONDS=5
STATE_PATH=/data/state.json
LOG_PATH=/data/trades.csv
